<h1>Account Type Details</h1>
<table>
<thead>
	<td> Account Title </td>
	<td> Amount </td>
</thead>
<tbody>
<% @accounts.each do |account| %>
<% if account.account_type.name == @accounttype.name %>
<tr>	
	<td><%= link_to "#{account.name}", account_path(account) %></td>
	<td><%= account.amount %></td> 
</tr>
<% end %>
<% end %>
</tbody>
</table>
